//I create an enum folder to make it easier to add future enums if needed
//This pageType enum is responsible to help control displaying the todo items in the correct column
export enum pageType {
	completed,
	todo,
}
